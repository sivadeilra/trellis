/*
use std::collections::HashMap;
use std::sync::Arc;

pub struct Graph {
    pub edges: Vec<Edge>,
    pub nodes: Vec<Node>,
}

pub struct Edge {
    pub f: usize,
    pub t: usize,
    pub attrs: Attributes,
}

pub struct Node {
    pub name: Option<Arc<String>>,
    pub attrs: Attributes,
}

impl Graph {
    pub fn add_edge(&mut self) -> &Edge {}
}

impl Node {}

impl Edge {}

pub struct Attributes {
    pub attrs: HashMap<String, String>,
}
*/
